void main() {
  // == > Arithmatic Operator < ==

  // int a = 20;
  // int b = 3;

  // print(a + b);
  // print(a - b);
  // print(a * b);
  // print(a / b);
  // print(a ~/ b);
  // print(a % b);

  // == > Relational Operator < ==

  // int x = 5;
  // int y = 3;

  // print(x == y);
  // print(x != y);
  // print(x >= y);
  // print(x <= y);
  // print(x > y);
  // print(x < y);

  // == > Logical Operators < ===

  // int a = 10;
  // int b = 20;
  // int c = 15;

  // print((a <= b) && (a < c));
  // print((a <= b) || (a > c));
  // print(!(a < b));

  // == > Assignment OPerator < ==

  // int x = 5;
  // x += 2;
  // x = x + 2;

  // == > Incre and decre OPerator < ==

  // int count = 4;
  // count++; // count = count + 1
  // print(count);

  // == >Null Aware OPerator < ==
  // String? username;
  // username = "Ishfaq";
  // print(username ?? 'Guest');
}
